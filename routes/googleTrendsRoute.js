const express = require("express");
const router = express.Router();

// Route for adding a new button
router.get("/getGoogleTrends", (req, res) => {});

// Exports the router to be used in the main application
module.exports = router;
